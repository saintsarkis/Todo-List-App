// now we have access to the header in JavaScript... we probably have no need for this
const header = document.querySelector('header');
// now we have access to the main section in JavaScript
const body = document.querySelector('section');
// now we have access to the footer in JavaScript... we'll most likely delete this later
const footer = document.querySelector('footer');

